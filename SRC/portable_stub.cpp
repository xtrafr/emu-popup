#include <iostream>

int main() {
    std::cout << "This project targets Windows only. Build performed on a non-Windows host.\n";
    std::cout << "The Windows-specific executables are not available on this platform." << std::endl;
    return 0;
}